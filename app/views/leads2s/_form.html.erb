<%= form_for(@leads2) do |f| %>
  <% if @leads2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leads2.errors.count, "error") %> prohibited this leads2 from being saved:</h2>

      <ul>
      <% @leads2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class = "field">
  <%= select_tag("leads2[status]", options_for_select([['Hot Sale', 1], ['Cold Sale', 2], ['Complete', 3], ['In-Development', 4]])) %>
  </div>
  
  <div class="field">
    <%= f.label :lead_developer %><br>
    <%= f.text_field :lead_developer %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="field">
    <%= f.label :hourly_rate %><br>
    <%= f.text_field :hourly_rate %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.date_select :deadline %>
  </div>
  <div class="actions">
    <%= f.submit "Create Lead" %>
  </div>
<% end %>
